import { StuctAttrDemoDirective } from './stuct-attr-demo.directive';

describe('StuctAttrDemoDirective', () => {
  it('should create an instance', () => {
    const directive = new StuctAttrDemoDirective();
    expect(directive).toBeTruthy();
  });
});
