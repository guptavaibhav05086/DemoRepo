import { AttrDirTrainingDirective } from './attr-dir-training.directive';

describe('AttrDirTrainingDirective', () => {
  it('should create an instance', () => {
    const directive = new AttrDirTrainingDirective();
    expect(directive).toBeTruthy();
  });
});
